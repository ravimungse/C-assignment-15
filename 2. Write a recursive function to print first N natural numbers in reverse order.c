//2. Write a recursive function to print first N natural numbers in reverse order
#include<stdio.h>
#include<conio.h>
main()
{
	int number;
	printf("\n Enter The number = ");
	scanf("%d",&number);
	reverse(number);
	getch();
}
